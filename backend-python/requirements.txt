# StockPilot Python backend — runtime dependencies.
# Pinned to compatible minimum versions; bump deliberately.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
httpx>=0.27,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
pandas>=2.2,<3.0
numpy>=1.26,<3.0

# Database — SQLAlchemy async core + PostgreSQL drivers.
# asyncpg is the async driver used at runtime; psycopg2-binary is the sync
# driver used only by Alembic migrations (which run synchronously).
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0
alembic>=1.13,<2.0
psycopg2-binary>=2.9,<3.0

# Scheduler — daily EOD ingestion job after GPW market close.
apscheduler>=3.10,<4.0

# Market data — Yahoo Finance as the primary OHLCV source (stooq.pl backup).
# GPW tickers on Yahoo Finance use the .WA suffix (e.g. PKO.WA).
yfinance>=0.2,<1.0
