# Keep the build context small and secrets out of the image.
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/

# Local credentials — production values come from environment variables.
.env

# Tests and docs are not needed at runtime.
tests/
README.md
requirements-dev.txt
